/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        locales: ['ru'], // Mavjud tillar
        defaultLocale: 'ru',  // Sukut bo'yicha til
    },
    reactStrictMode: false,
};

export default nextConfig;
